<Window x:Class="FastCli.Desktop.Views.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入"
        Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#FFFDF9F2">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageTextBlock"
                   Foreground="#4F4639"
                   TextWrapping="Wrap" />

        <TextBox x:Name="InputTextBox"
                 Grid.Row="1"
                 Margin="0,14,0,18"
                 Padding="8,6" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Margin="0,0,8,0" Padding="12,6" Click="CancelButton_Click">取消</Button>
            <Button Padding="12,6" IsDefault="True" Click="OkButton_Click">确定</Button>
        </StackPanel>
    </Grid>
</Window>
