<Window x:Class="FastCli.Desktop.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置"
        Width="560"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        FontFamily="Microsoft YaHei UI, Segoe UI Variable Display, Segoe UI, sans-serif"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SecondaryButtonBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryButtonForegroundBrush}" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SecondaryButtonHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource PrimaryButtonBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryButtonForegroundBrush}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryButtonHoverBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryBorderlessButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
            <Setter Property="Padding" Value="8,6" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ControlHoverBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource PanelBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="18" />
        </Style>

        <Style x:Key="ModernToggleSwitch" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border x:Name="SwitchBackground"
                                    Width="40"
                                    Height="22"
                                    CornerRadius="11"
                                    Background="{DynamicResource ControlBorderBrush}"
                                    BorderThickness="1"
                                    BorderBrush="{DynamicResource ControlBorderBrush}">
                                <Ellipse x:Name="SwitchThumb"
                                         Width="14"
                                         Height="14"
                                         Fill="{DynamicResource PanelBackgroundBrush}"
                                         HorizontalAlignment="Left"
                                         Margin="3,0,0,0">
                                    <Ellipse.Effect>
                                        <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.2" />
                                    </Ellipse.Effect>
                                </Ellipse>
                            </Border>
                            <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="SwitchBackground" Property="Background" Value="{DynamicResource PrimaryButtonBackgroundBrush}" />
                                <Setter TargetName="SwitchBackground" Property="BorderBrush" Value="{DynamicResource PrimaryButtonBackgroundBrush}" />
                                <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right" />
                                <Setter TargetName="SwitchThumb" Property="Margin" Value="0,0,3,0" />
                                <Setter TargetName="SwitchThumb" Property="Fill" Value="{DynamicResource PrimaryButtonForegroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="设置" FontSize="26" FontWeight="Bold" />
            <TextBlock Margin="0,6,0,0"
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       Text="主题、版本和更新相关配置都集中在这里。" />
        </StackPanel>

        <Border Grid.Row="1"
                Margin="0,18,0,0"
                Style="{StaticResource CardBorderStyle}">
            <StackPanel>
                <TextBlock Text="外观" FontSize="16" FontWeight="Bold" />
                <Border Margin="0,14,0,0"
                        Padding="12"
                        Background="{DynamicResource ControlBackgroundBrush}"
                        BorderBrush="{DynamicResource ControlBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                    <CheckBox Content="启用深色主题"
                              IsChecked="{Binding IsDarkTheme, Mode=TwoWay}"
                              Style="{StaticResource ModernToggleSwitch}" />
                </Border>
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                Margin="0,14,0,0"
                Style="{StaticResource CardBorderStyle}">
            <StackPanel>
                <TextBlock Text="版本与更新" FontSize="16" FontWeight="Bold" />

                <Grid Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="当前版本"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               VerticalAlignment="Center" />
                    <Border Grid.Column="1"
                            Margin="14,0,0,0"
                            Padding="10,4"
                            HorizontalAlignment="Left"
                            Background="{DynamicResource ControlBackgroundBrush}"
                            BorderBrush="{DynamicResource ControlBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="999">
                        <TextBlock Text="{Binding CurrentVersion}" FontWeight="SemiBold" />
                    </Border>
                </Grid>

                <TextBlock Margin="0,10,0,0"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           Text="{Binding SkippedVersionText}" />

                <WrapPanel Margin="0,16,0,0">
                    <Button Content="检查更新"
                            Width="104"
                            Margin="0,0,10,10"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Click="CheckUpdateButton_Click" />
                    <Button Content="清除忽略"
                            Width="104"
                            Margin="0,0,10,10"
                            Click="ClearSkippedVersionButton_Click" />
                    <Button Content="打开 Releases"
                            Width="118"
                            Margin="0,0,0,10"
                            Style="{StaticResource SecondaryBorderlessButton}"
                            Click="OpenReleasesButton_Click" />
                </WrapPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="3"
                Margin="0,14,0,0"
                Style="{StaticResource CardBorderStyle}">
            <StackPanel>
                <TextBlock Text="说明" FontSize="16" FontWeight="Bold" />
                <TextBlock Margin="0,14,0,0"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           LineHeight="22"
                           Text="应用启动后会自动检查 GitHub Releases。若你在更新弹窗里选择“否”，当前版本会被记为已忽略，直到出现更高版本才会再次提示。" />
            </StackPanel>
        </Border>

        <DockPanel Grid.Row="4" Margin="0,18,0,0">
            <TextBlock Text="修改会立即生效"
                       Foreground="{DynamicResource TertiaryTextBrush}"
                       VerticalAlignment="Center"
                       DockPanel.Dock="Left" />
            <Button Width="104"
                    Content="关闭"
                    IsDefault="True"
                    HorizontalAlignment="Right"
                    DockPanel.Dock="Right"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="CloseButton_Click" />
        </DockPanel>
    </Grid>
</Window>
