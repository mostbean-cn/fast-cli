<UserControl x:Class="FastCli.Desktop.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:FastCli.Desktop.Localization"
             Background="{DynamicResource WindowBackgroundBrush}"
             Foreground="{DynamicResource PrimaryTextBrush}"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True"
             FontFamily="Microsoft YaHei UI, Segoe UI Variable Display, Segoe UI, sans-serif"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SecondaryButtonBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryButtonForegroundBrush}" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SecondaryButtonHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource PrimaryButtonBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryButtonForegroundBrush}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryButtonHoverBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryBorderlessButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
            <Setter Property="Padding" Value="8,6" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ControlHoverBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BackButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryBorderlessButton}">
            <Setter Property="Padding" Value="10,8" />
        </Style>

        <Style x:Key="ButtonGlyphTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe Fluent Icons, Segoe MDL2 Assets" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
        </Style>

        <Style x:Key="ButtonLabelTextStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
        </Style>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource PanelBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="18" />
        </Style>

        <Style x:Key="SettingOptionCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="14" />
        </Style>

        <Style x:Key="LanguageComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Margin="4,3"
                                Padding="12,10"
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="1"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ControlHoverBrush}" />
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource PanelBorderBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource SelectedItemBackgroundBrush}" />
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource SelectedItemBorderBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="LanguageSelectionTemplate">
            <TextBlock Text="{Binding Label}"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center" />
        </DataTemplate>

        <ControlTemplate x:Key="LanguageComboBoxToggleButton" TargetType="ToggleButton">
            <Border x:Name="TemplateRoot"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <ContentPresenter Grid.Column="0"
                                      Margin="{TemplateBinding Padding}"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Stretch" />

                    <Path x:Name="Arrow"
                          Grid.Column="1"
                          Margin="12,0,14,0"
                          VerticalAlignment="Center"
                          Data="M 0 0 L 5 5 L 10 0"
                          Stroke="{TemplateBinding Foreground}"
                          StrokeThickness="2"
                          StrokeStartLineCap="Round"
                          StrokeEndLineCap="Round"
                          Stretch="None" />
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="TemplateRoot" Property="BorderBrush" Value="{DynamicResource SelectedItemBorderBrush}" />
                    <Setter TargetName="TemplateRoot" Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="TemplateRoot" Property="BorderBrush" Value="{DynamicResource SelectedItemBorderBrush}" />
                    <Setter TargetName="Arrow" Property="Data" Value="M 0 5 L 5 0 L 10 5" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="LanguageComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="0,10,0,10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Height" Value="58" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource LanguageComboBoxItemStyle}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{StaticResource LanguageSelectionTemplate}"
                                          Foreground="{TemplateBinding Foreground}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Padding="14,10,0,10"
                                          Template="{StaticResource LanguageComboBoxToggleButton}" />

                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Bottom"
                                   PopupAnimation="Slide">
                                <Border Margin="0,6,0,0"
                                        MinWidth="{Binding ActualWidth, ElementName=ToggleButton}"
                                        Background="{DynamicResource PanelBackgroundBrush}"
                                        BorderBrush="{DynamicResource PanelBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="10">
                                    <ScrollViewer Margin="0,4"
                                                  CanContentScroll="True"
                                                  VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="ToggleButton" Property="BorderBrush" Value="{DynamicResource ControlFocusedBorderBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="LanguageOptionTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding Label}" FontWeight="SemiBold" />
                    <TextBlock Margin="0,3,0,0"
                               Text="{Binding Description}"
                               FontSize="11"
                               Foreground="{DynamicResource SecondaryTextBrush}" />
                </StackPanel>

                <Border Grid.Column="1"
                        Margin="12,0,0,0"
                        Padding="8,4"
                        VerticalAlignment="Center"
                        Background="{DynamicResource SelectedItemBackgroundBrush}"
                        BorderBrush="{DynamicResource SelectedItemBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="999">
                    <TextBlock Text="{Binding Meta}"
                               FontSize="10"
                               FontWeight="Bold"
                               Foreground="{DynamicResource SecondaryTextBrush}" />
                </Border>
            </Grid>
        </DataTemplate>

        <Style x:Key="ModernToggleSwitch" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border x:Name="SwitchBackground"
                                    Width="40"
                                    Height="22"
                                    CornerRadius="11"
                                    Background="{DynamicResource ControlBorderBrush}"
                                    BorderThickness="1"
                                    BorderBrush="{DynamicResource ControlBorderBrush}">
                                <Ellipse x:Name="SwitchThumb"
                                         Width="14"
                                         Height="14"
                                         Fill="{DynamicResource PanelBackgroundBrush}"
                                         HorizontalAlignment="Left"
                                         Margin="3,0,0,0">
                                    <Ellipse.Effect>
                                        <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.2" />
                                    </Ellipse.Effect>
                                </Ellipse>
                            </Border>
                            <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="SwitchBackground" Property="Background" Value="{DynamicResource PrimaryButtonBackgroundBrush}" />
                                <Setter TargetName="SwitchBackground" Property="BorderBrush" Value="{DynamicResource PrimaryButtonBackgroundBrush}" />
                                <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right" />
                                <Setter TargetName="SwitchThumb" Property="Margin" Value="0,0,3,0" />
                                <Setter TargetName="SwitchThumb" Property="Fill" Value="{DynamicResource PrimaryButtonForegroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <Button HorizontalAlignment="Left"
                        Style="{StaticResource BackButtonStyle}"
                        Click="BackButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72B;" FontSize="13" Margin="0,0,6,0" Style="{StaticResource ButtonGlyphTextStyle}" />
                        <TextBlock Text="{loc:Loc Common_Back}" FontWeight="SemiBold" Style="{StaticResource ButtonLabelTextStyle}" />
                    </StackPanel>
                </Button>
                <TextBlock Margin="0,14,0,0"
                           Text="{loc:Loc Settings_Header}"
                           FontSize="26"
                           FontWeight="Bold" />
                <TextBlock Margin="0,6,0,0"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           Text="{loc:Loc Settings_Subtitle}" />
            </StackPanel>

            <Border Grid.Row="1"
                    Margin="0,18,0,0"
                    Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="{loc:Loc Settings_Appearance}" FontSize="16" FontWeight="Bold" />
                    <Border Margin="0,14,0,0"
                            Style="{StaticResource SettingOptionCardStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Margin="0,0,14,0">
                                <TextBlock Text="{loc:Loc Settings_Theme}" FontWeight="Bold" />
                                <TextBlock Margin="0,4,0,0"
                                           Text="{loc:Loc Settings_ThemeHint}"
                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                           TextWrapping="Wrap" />
                            </StackPanel>

                            <CheckBox Grid.Column="1"
                                      VerticalAlignment="Center"
                                      Content="{loc:Loc Settings_DarkTheme}"
                                      IsChecked="{Binding IsDarkTheme, Mode=TwoWay}"
                                      Style="{StaticResource ModernToggleSwitch}" />
                        </Grid>
                    </Border>
                    <Border Margin="0,12,0,0"
                            Style="{StaticResource SettingOptionCardStyle}">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc Settings_Language}" FontWeight="Bold" />
                            <TextBlock Margin="0,4,0,0"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       TextWrapping="Wrap"
                                       Text="{loc:Loc Settings_LanguageHint}" />
                            <ComboBox Margin="0,8,0,0"
                                      PreviewMouseWheel="LanguageComboBox_PreviewMouseWheel"
                                      Style="{StaticResource LanguageComboBoxStyle}"
                                      ItemsSource="{Binding AvailableLanguages}"
                                      ItemTemplate="{StaticResource LanguageOptionTemplate}"
                                      SelectedItem="{Binding SelectedLanguageOption, Mode=TwoWay}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <Border Grid.Row="2"
                    Margin="0,14,0,0"
                    Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="{loc:Loc Settings_VersionAndUpdates}" FontSize="16" FontWeight="Bold" />
                    <Border Margin="0,14,0,0"
                            Style="{StaticResource SettingOptionCardStyle}">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc Settings_UpdateStatus}" FontWeight="Bold" />
                            <TextBlock Margin="0,4,0,0"
                                       Text="{loc:Loc Settings_UpdateStatusHint}"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       TextWrapping="Wrap" />
                            <TextBlock Margin="0,6,0,0"
                                       Text="{loc:Loc Settings_UpdateDescription}"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       TextWrapping="Wrap"
                                       LineHeight="22" />

                            <Grid Margin="0,12,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{loc:Loc Settings_CurrentVersion}"
                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                           VerticalAlignment="Center" />
                                <Border Grid.Column="1"
                                        Margin="14,0,0,0"
                                        Padding="10,4"
                                        HorizontalAlignment="Left"
                                        Background="{DynamicResource PanelBackgroundBrush}"
                                        BorderBrush="{DynamicResource ControlBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="999">
                                    <TextBlock Text="{Binding CurrentVersion}" FontWeight="SemiBold" />
                                </Border>
                            </Grid>

                            <Border Margin="0,12,0,0"
                                    Padding="12,10"
                                    Background="{DynamicResource PanelBackgroundBrush}"
                                    BorderBrush="{DynamicResource ControlBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8">
                                <TextBlock Foreground="{DynamicResource SecondaryTextBrush}"
                                           Text="{Binding SkippedVersionText}" />
                            </Border>
                        </StackPanel>
                    </Border>

                    <Border Margin="0,12,0,0"
                            Style="{StaticResource SettingOptionCardStyle}">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc Settings_UpdateActions}" FontWeight="Bold" />
                            <TextBlock Margin="0,4,0,0"
                                       Text="{loc:Loc Settings_UpdateActionsHint}"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       TextWrapping="Wrap" />

                            <WrapPanel Margin="0,12,0,0">
                                <Button Content="{loc:Loc Common_CheckUpdates}"
                                        MinWidth="118"
                                        Margin="0,0,10,10"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Click="CheckUpdateButton_Click" />
                                <Button Content="{loc:Loc Settings_ClearSkipped}"
                                        MinWidth="118"
                                        Margin="0,0,10,10"
                                        Click="ClearSkippedVersionButton_Click" />
                                <Button Content="{loc:Loc Common_OpenReleases}"
                                        MinWidth="132"
                                        Margin="0,0,0,10"
                                        Style="{StaticResource SecondaryBorderlessButton}"
                                        Click="OpenReleasesButton_Click" />
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <DockPanel Grid.Row="3" Margin="0,18,0,0">
                <TextBlock Text="{loc:Loc Settings_ApplyImmediately}"
                           Foreground="{DynamicResource TertiaryTextBrush}"
                           VerticalAlignment="Center"
                           DockPanel.Dock="Left" />
            </DockPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
